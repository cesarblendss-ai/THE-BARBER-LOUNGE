# Notes (optional)

Scratch space for stable reference notes that don't fit other org-convention files.

Agents: **read-only** — do not add session logs here. Use `docs/team-memory/` for working state.
